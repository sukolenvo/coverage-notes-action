name: test
on:
  push:

jobs:
  coverage-notes:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Cache
        uses: ./
        with:
          jacoco: sample-jacoco.xml
          notes_ref: ${{ matrix.os }}
